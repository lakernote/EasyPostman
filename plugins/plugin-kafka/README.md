# Plugin Kafka

Kafka 已拆分为独立插件模块：

- Kafka Toolbox UI
- `pm.kafka` / `pm.plugin("kafka")` 脚本 API
- 自动补全与 Snippet
- 插件 descriptor 与离线/在线安装产物

构建产物：

- `target/easy-postman-${project.version}-plugin-kafka.jar`
- `target/plugin-market/offline/easy-postman-plugin-kafka/`
