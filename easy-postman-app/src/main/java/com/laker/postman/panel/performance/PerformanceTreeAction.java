package com.laker.postman.panel.performance;

enum PerformanceTreeAction {
    ADD_THREAD_GROUP,
    ADD_REQUEST,
    ADD_LOOP,
    ADD_SSE_CONNECT,
    ADD_SSE_AWAIT,
    ADD_WS_CONNECT,
    ADD_WS_SEND,
    ADD_WS_AWAIT,
    ADD_WS_CLOSE,
    ADD_ASSERTION,
    ADD_TIMER,
    ENABLE,
    DISABLE,
    COPY,
    PASTE,
    RENAME,
    DELETE
}
